| 用例ID | 标题 | 模块 | 测试类型 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|--------|------|------|---------|--------|---------|---------|---------|
| TC-CHPWD-001 | 验证正确原密码修改密码成功 | 修改密码 | 功能测试 | P0 | 1. 已注册用户，当前密码为 current123 2. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"current123","newPassword":"newPass456"} | 1. 状态码 200 2. 返回 {"message":"密码修改成功"} 3. 使用新密码 newPass456 可以登录成功 4. 使用旧密码 current123 登录失败 |
| TC-CHPWD-002 | 验证修改密码后旧 token 失效 | 修改密码 | 功能测试 | P1 | 1. 已注册用户，当前密码为 current123 2. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"current123","newPassword":"newPass456"} 2. 用同一个 token 发送 GET /api/auth/me（或任意需认证接口） | 1. 步骤1状态码 200 2. 步骤2状态码 401，旧 token 已失效 |
| TC-CHPWD-003 | 验证原密码错误返回 422 | 修改密码 | 异常场景 | P1 | 1. 已登录获取有效 token，当前密码为 current123 | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"wrongPassword","newPassword":"newPass456"} | 1. 状态码 422 2. 返回 error.code = "WRONG_PASSWORD" 3. 返回 error.message = "原密码错误" 4. 原密码未被修改 |
| TC-CHPWD-004 | 验证未携带 token 返回 401 | 修改密码 | 异常场景 | P1 | 无 | 1. 发送 POST /api/auth/change-password，不设置 Authorization Header，body: {"oldPassword":"current123","newPassword":"newPass456"} | 1. 状态码 401 2. 返回 error.code = "UNAUTHORIZED" 3. 返回 error.message = "未登录或 token 已过期" |
| TC-CHPWD-005 | 验证 token 已过期返回 401 | 修改密码 | 异常场景 | P1 | 1. 已获取一个已过期的 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {expired_token}，body: {"oldPassword":"current123","newPassword":"newPass456"} | 1. 状态码 401 2. 返回 error.code = "UNAUTHORIZED" |
| TC-CHPWD-006 | 验证 token 无效返回 401 | 修改密码 | 异常场景 | P1 | 无 | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer invalid_token_abc，body: {"oldPassword":"current123","newPassword":"newPass456"} | 1. 状态码 401 2. 返回 error.code = "UNAUTHORIZED" |
| TC-CHPWD-007 | 验证新密码不足 6 位返回 422 | 修改密码 | 参数校验 | P1 | 1. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"current123","newPassword":"abc"} | 1. 状态码 422 2. 返回 error.code = "VALIDATION_ERROR" 3. 返回 error.message = "业务规则校验失败" |
| TC-CHPWD-008 | 验证新密码恰好 6 位通过校验 | 修改密码 | 边界条件 | P2 | 1. 已登录获取有效 token，当前密码为 current123 | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"current123","newPassword":"abcdef"} | 1. 状态码 200 2. 返回 {"message":"密码修改成功"} |
| TC-CHPWD-009 | 验证缺少 oldPassword 字段返回 422 | 修改密码 | 参数校验 | P1 | 1. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"newPassword":"newPass456"} | 1. 状态码 422 2. 返回 error.code = "VALIDATION_ERROR" |
| TC-CHPWD-010 | 验证缺少 newPassword 字段返回 422 | 修改密码 | 参数校验 | P1 | 1. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"current123"} | 1. 状态码 422 2. 返回 error.code = "VALIDATION_ERROR" |
| TC-CHPWD-011 | 验证空请求体返回 422 | 修改密码 | 边界条件 | P2 | 1. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {} | 1. 状态码 422 2. 返回 error.code = "VALIDATION_ERROR" |
| TC-CHPWD-012 | 验证 oldPassword 为空字符串返回 422 | 修改密码 | 边界条件 | P2 | 1. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"","newPassword":"newPass456"} | 1. 状态码 422 2. 返回 error.code = "VALIDATION_ERROR" |
| TC-CHPWD-013 | 验证 newPassword 为空字符串返回 422 | 修改密码 | 边界条件 | P2 | 1. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"current123","newPassword":""} | 1. 状态码 422 2. 返回 error.code = "VALIDATION_ERROR" |
| TC-CHPWD-014 | 验证新旧密码相同可以成功（接口未禁止） | 修改密码 | 业务规则 | P2 | 1. 已登录获取有效 token，当前密码为 current123 | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"current123","newPassword":"current123"} | 1. 状态码 200（如果接口未限制新旧密码相同） 或 状态码 422（如果接口限制） |
| TC-CHPWD-015 | 验证 Authorization 缺少 Bearer 前缀返回 401 | 修改密码 | 边界条件 | P2 | 1. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: {valid_token}（无 Bearer 前缀），body: {"oldPassword":"current123","newPassword":"newPass456"} | 1. 状态码 401 |
| TC-CHPWD-016 | 验证新密码包含特殊字符可以成功 | 修改密码 | 边界条件 | P2 | 1. 已登录获取有效 token，当前密码为 current123 | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，body: {"oldPassword":"current123","newPassword":"P@ss!#456"} | 1. 状态码 200 2. 返回 {"message":"密码修改成功"} |
| TC-CHPWD-017 | 验证请求 Content-Type 非 JSON 的处理 | 修改密码 | 边界条件 | P3 | 1. 已登录获取有效 token | 1. 发送 POST /api/auth/change-password，Header: Authorization: Bearer {valid_token}，Content-Type: text/plain，body: 纯文本 | 1. 状态码 400 或 422 |
